name: Hello Script

on:
 push:

jobs:
 greet:
  runs-on: ubuntu-latest
  
  steps:
   - name: code checkout
     uses: actions/checkout@v4
   
   - name: greet hello
     run: echo "Hello from git workflow"
